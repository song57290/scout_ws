내일 할 일

1. 핑퐁 문제 해결
2. 자율 주행 중 화재 토픽 발견 시 화재 토픽이 있는 곳을 Depth_camera 등을 이용하여 위치를 저장
3. 저장한 후 (0, 1, 0)으로 복귀하여 소화기 gripper로 잡기
3. 소화기를 gripper로 잡고나서 2번에 저장해두었던 화재 위치로 복귀




회전은 그대로 앞으로 가는건 20%정도 느리게 해주고 특히 아직도 핑퐁현상 수정이 안되고 있거든? 지속적으로 보면 너무 급격하게 벽에 붙다보니 전진속도를 주체하지 못하고 벗어나고 있어 회전시에도 nav2 경로선이 각져 있는 현상이 발생하는데 이것도 수정해주라 좀 더 nav2 경로 선이 완만하게 나타날 수 있게 이것과 같아

robot_radius 이걸 키우니 빨간색 없어지고 파란색  그리고 2번째로 가는 코스 직선 코스로 변함 대신 코드에서 붙음 현상 발생



local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 10.0 
      publish_frequency: 10.0
      global_frame: odom
      robot_base_frame: base_link
      transform_tolerance: 0.30
      use_sim_time: True
      rolling_window: true
      width: 10
      height: 10
      resolution: 0.05
      robot_radius: 0.35 # 0.85
      plugins: ["voxel_layer", "inflation_layer"]
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 2.0 # 작을 수록 멀리 퍼짐
        inflation_radius: 0.50 # 로봇 반경
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 9.5
          obstacle_min_range: 0.0
      static_layer:
        map_subscribe_transient_local: True
      always_send_full_costmap: True
  local_costmap_client:
    ros__parameters:
      use_sim_time: True
  local_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 10.0
      publish_frequency: 10.0
      global_frame: map
      robot_base_frame: base_link
      transform_tolerance: 0.30
      use_sim_time: True
      robot_radius: 0.45 # 0.85
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 9.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 2.0 # 작을 수록 멀리 퍼짐
        inflation_radius: 0.50 # 로봇 반경
      always_send_full_costmap: True
  global_costmap_client:
    ros__parameters:
      use_sim_time: True
  global_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: True

